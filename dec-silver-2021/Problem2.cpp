#include <bits/stdc++.h>
using namespace std;

int MAX_M = pow(10, 5);

int main() {
    int T;
    cin >> T;

    for (int test=0; test<T; test++) {
        int N, M;
        cin >> N >> M;
    }
    return 0;
}